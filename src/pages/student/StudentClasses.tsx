// Legacy compatibility wrapper.
// Keep this import path safe for any remaining references while using the
// corrected implementation that matches the production terms table.
export { default } from './StudentClassesFixed';
